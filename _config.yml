# Build settings

port: 4000
markdown:  kramdown
permalink: /:title
plugins:   [jekyll-sitemap, jekyll-feed,  jekyll-seo-tag]

# Customise atom feed settings (this is where Jekyll-Feed gets configuration information)

title:		   "Arcade Bridge"
description: "We love playing games. We help you to release your next game faster, so we can play it!"
author:      "Arcade Bridge"
